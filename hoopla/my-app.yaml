apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
  namespace: hoopla
  labels:
    app: my-app
    managed-by: portainer-idp
  annotations:
    portainer-idp/git-target-id: '1'
    portainer-idp/git-branch: main
    portainer-idp/git-path: hoopla/my-app.yaml
    portainer-idp/edge-stack: 'true'
    portainer-idp/edge-stack-id: '1'
    portainer-idp/edge-group-ids: '1'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
        managed-by: portainer-idp
    spec:
      containers:
        - name: app
          image: nginx:latest
          imagePullPolicy: Always
